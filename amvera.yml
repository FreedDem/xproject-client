meta:
  environment: node
  toolchain:
    name: npm
    version: "20"

build:
  skip: false
  additionalCommands: |
    set -e
    echo "=== 🚀 BUILD ==="
    node -v && npm -v
    rm -rf dist
    npm ci
    # если нужен свой API-бекенд — пропиши тут переменную окружения для Vite
    VITE_API_BASE=https://xproject-server-freede.amvera.io npm run build
    echo "=== 📦 DIST CONTENT ==="
    ls -la dist || true

run:
  # Запускаем напрямую serve и слушаем $PORT
  command: npx serve -s dist -l $PORT
